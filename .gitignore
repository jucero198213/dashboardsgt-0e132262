# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri — binários compilados (não versionar)
src-tauri/target/
src-tauri/gen/

# Environment variables
# root .env — contém apenas anon keys públicas do Supabase, seguro commitar
# dw-api-local/.env — contém senha do SQL Server, NUNCA commitar
dw-api-local/.env
.env.local
.env.production
.env.*.local
