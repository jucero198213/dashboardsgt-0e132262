/**
 * HomeButton — descontinuado.
 * A navegação agora vive na AppSidebar global. Mantido como stub para
 * compatibilidade com páginas que ainda o importam.
 */
export function HomeButton() {
  return null;
}
